$(function() {
  $('.carousel').slick({
    dots: true,
    infinite: true,
    speed: 400,
    fade: true,
    cssEase: 'linear',
    // slidesToShow: 1,
    // centerMode: true,
    // variableWidth: true
  });
});

